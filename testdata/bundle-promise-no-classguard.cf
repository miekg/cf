bundle agent bar {
    classes:

      "/etc/prometheus/prometheus.yml"
        classes => if_repaired("prometheus_updated");

      "/etc/prometheus/prometheus.yml"
        classes => if_repaired("prometheus_updated");
}
