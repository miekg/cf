body perms system(bla, root, bla)
{
      mode => "644";
      owners => { "root" };
      groups => { "root" };
}

###################################

body perms system(bla, root, bla)
{
    # dag
    All::
      mode => "644";
      owners => { "root" };
      # more comments
      # another one
      groups => { "root" };
}
