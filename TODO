TODO:

1. long lists break them up
2. thinarrow
3. comments in lists (probably parsed, can output them as well - as we can detect them)
   - need items over multiple lines
   - incinga.cf comment in list is such a file
